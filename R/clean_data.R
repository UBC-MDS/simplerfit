#'Clean_data
#'Load downloaded data, clean the dataset, remove the NA score. 
#'Clean data(remove Nan rows, strip extra white spaces from column names, and data, convert all column names to lower case, etc)
#'
#' @param  data.frame Data set to clean
#'
#' @return data.frame A cleaned and simplified DataFrame of the relevant columns for summary and visualization
#' @export
#'
#' @examples
#' df <- clean_data(data)

clean_data <- function(data.frame){}
